import logging

def log_airflow_context(**kwargs):
    """
    Logs useful Airflow context variables from kwargs.
    Can be reused in multiple tasks for debugging and observability.
    Also logs Dataflow job info if present in XComs.
    """
    log = logging.getLogger("airflow.task")

    # Basic Airflow context
    log.info("🔹 DAG ID: %s", kwargs["dag"].dag_id)
    log.info("🔹 Task ID: %s", kwargs["task"].task_id)
    log.info("🔹 Run ID: %s", kwargs["dag_run"].run_id)
    log.info("🔹 Execution Date (datetime): %s", kwargs["execution_date"])
    log.info("🔹 DS (date string): %s", kwargs["ds"])
    log.info("🔹 Next Execution Date: %s", kwargs.get("next_execution_date"))
    log.info("🔹 Previous Execution Date: %s", kwargs.get("prev_execution_date"))
    log.info("🔹 Params: %s", kwargs.get("params", {}))

    # Task instance info
    ti = kwargs["ti"]
    log.info("🔹 Try Number: %s", ti.try_number)
    log.info("🔹 Task State: %s", ti.state)

    # Check if this task launched a Dataflow Flex Template
    job_info = ti.xcom_pull(task_ids=ti.task_id)
    if job_info and isinstance(job_info, dict):
        job_id = job_info.get("job", {}).get("id")
        project = job_info.get("job", {}).get("projectId")
        region = job_info.get("job", {}).get("location")

        if job_id:
            log.info("🚀 Dataflow Job launched:")
            log.info("   • Job ID: %s", job_id)
            log.info("   • Project: %s", project)
            log.info("   • Region: %s", region)
            log.info("   • Console URL: https://console.cloud.google.com/dataflow/jobs/%s/%s?project=%s",
                     region, job_id, project)
